#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../yeoman/bin/yeoman" "$@"
  ret=$?
else 
  node  "$basedir/../yeoman/bin/yeoman" "$@"
  ret=$?
fi
exit $ret
