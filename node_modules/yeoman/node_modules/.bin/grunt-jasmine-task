#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-jasmine-task/bin/grunt-jasmine-task" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-jasmine-task/bin/grunt-jasmine-task" "$@"
  ret=$?
fi
exit $ret
